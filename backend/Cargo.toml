[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
cxx = "1.0"
proc-bitfield = "0.3"

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
cxx-build = "1.0"
